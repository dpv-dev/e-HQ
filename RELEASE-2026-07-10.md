# Release 2026-07-10

## Scope delivered

- Finance/domain integration finalized in API and domain packages.
- Login route unified with landing UX (`/login` renders landing + forced sign-in overlay).
- Distribution API client gaps closed (allocations details, payee detail/create, payee partner-link, FX rates).
- Office projects edit data-loss risk fixed by exposing and reusing write fields (`writeStatus`, `description`, `active`).
- Design System cleanup completed (`View components` CTA wired, hardcoded sizes tokenized).
- Legacy office bulk-upsert dead client methods removed; API aliases documented as backward-compatible.

## Validation executed

- Full pipeline green via `./deploy-build.sh`.
- API tests: 65/65 pass.
- `@ehq/api-client` check: pass.
- `@ehq/api` check: pass.
- `@ehq/hq` check: pass (0 errors, 0 warnings).

## Production deployment status

- API deployed and restarted on `api.eeee.mu`.
- Frontend deployed on `app.eeee.mu`.
- Smoke checks: `healthz=200`, app routes `200`, protected API routes `401` unauthenticated as expected.
- Live frontend assets observed after deploy: `index-BWeU4O6_.css`, `index-CqDEtW3Z.js`.
- Runtime logs sampled on host: no immediate error burst observed.

## Post-release monitoring (non-blocking)

- Keep short watch on API logs (`console.log`, `stderr.log`) for repeated 5xx spikes.
- Spot-check user flows in Office/Distribution/Command Center after business traffic picks up.
- Keep alias routes for ledger bulk endpoints until all consumers are confirmed migrated.
