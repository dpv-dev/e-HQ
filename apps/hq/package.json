{
  "name": "@ehq/hq",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "svelte-check --tsconfig ./tsconfig.svelte-check.json && tsc -b --pretty false",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run"
  },
  "dependencies": {
    "@ehq/api-client": "workspace:*",
    "@ehq/auth": "workspace:*",
    "@ehq/ui": "workspace:*",
    "@supabase/supabase-js": "^2.108.2",
    "pdfjs-dist": "4.7.76"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "svelte-check": "^4.7.1",
    "vitest": "^3.2.4"
  }
}
